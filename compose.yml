services:
  mysql:
    image: mysql:8.0
    container_name: vitalapp-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root1234
      MYSQL_DATABASE: vital_app_db
      MYSQL_USER: vitalapp
      MYSQL_PASSWORD: vitalapp123
      TZ: America/Bogota
    ports:
      - "3307:3306"  # Puerto externo 3307 para evitar conflicto con MySQL local
    volumes:
      - mysqldata:/var/lib/mysql
      - ./Backend/src/main/resources/sql:/docker-entrypoint-initdb.d:ro
    networks:
      - vitalapp-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-proot1234"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    command: --default-authentication-plugin=mysql_native_password --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci

  # Backend Spring Boot
  backend:
    build:
      context: ./Backend
      dockerfile: Dockerfile
    container_name: vitalapp-backend
    environment:
      SPRING_PROFILES_ACTIVE: docker
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/vital_app_db?useSSL=false&serverTimezone=America/Bogota&allowPublicKeyRetrieval=true
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: root1234
      TZ: America/Bogota
      JAVA_OPTS: "-Xms256m -Xmx512m"
    ports:
      - "8080:8080"
    networks:
      - vitalapp-network
    depends_on:
      mysql:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 90s
    restart: unless-stopped

  # Opcional: navegador de BD súper simple
  adminer:
    image: adminer
    container_name: vitalapp-adminer
    ports:
      - "8082:8080"
    networks:
      - vitalapp-network
    depends_on:
      - mysql
    restart: unless-stopped

volumes:
  mysqldata:

networks:
  vitalapp-network:
    driver: bridge
